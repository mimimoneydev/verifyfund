{
  "name": "hedera-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "HBAR-native Campaign contract (payable donate) for Hedera Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network hederaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}

